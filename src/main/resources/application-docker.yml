spring:
  application:
    name: device-manager
  datasource:
    url: jdbc:postgresql://postgres:5432/device_db
    username: device_user
    password: device_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true